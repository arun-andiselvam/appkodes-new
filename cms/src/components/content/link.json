{
  "collectionName": "components_content_links",
  "info": {
    "displayName": "Link",
    "description": "A phrase in the paragraph above, and where it points. The site finds the phrase and wraps it, so the copy stays readable as prose."
  },
  "options": {},
  "attributes": {
    "phrase": {
      "type": "string",
      "required": true
    },
    "href": {
      "type": "string",
      "required": true
    }
  }
}
